{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "penpot-design": {
      "type": "remote",
      "url": "http://192.168.31.111:4401/sse",
      "enabled": true,
      "timeout": 30000,
      "description": "Penpot MCP 设计服务器 — SSE 端点 (type: remote)。LAN 部署时改为 http://<LAN_IP>:4401/sse。如使用 Streamable HTTP 则将 type 改为 'http' 且 url 用 /mcp"
    }
  }
}
